# Add to INSTALLED_APPS
INSTALLED_APPS = [
    ...,
    "corsheaders",
]

# Add to MIDDLEWARE
MIDDLEWARE = [
    ...,
    "corsheaders.middleware.CorsMiddleware",  # Should be near the top
    "django.middleware.common.CommonMiddleware",
]

# CORS Configuration
CORS_ALLOWED_ORIGINS = [
    "https://clientnest.xyz",
    "https://www.clientnest.xyz",
    "https://www.api.clientnest.xyz",
    "http://localhost:3000",
    # Optionally add your Vercel preview domain(s)
    # "https://frontend-client-nest.vercel.app/",
]

CORS_ALLOW_CREDENTIALS = True  # For cookies/sessions